apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: storage
  labels:
    app: minio
spec:
  clusterIP: None  
  ports:
    - port: 9000
      name: minio
    - port: 9001
      name: console
  selector:
    app: minio