apiVersion: apps/v1
kind: Deployment
metadata:
  name: airflow-triggerer
  namespace: orchestration
spec:
  selector:
    matchLabels: &id001
      app: airflow-triggerer
      role: triggerer
  template:
    metadata:
      labels: *id001
    spec:
      containers:
      - name: airflow-triggerer
        image: nlhtungg/mbs-poc-airflow:1.0.0
        env:
        - name: AIRFLOW__CORE__EXECUTOR
          value: CeleryExecutor
        command:
        - airflow
        - triggerer
  replicas: 1
